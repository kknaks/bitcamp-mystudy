package bitcamp.myapp.command;

public class User {

  // Non static 필드(field; 변수) = 인스턴스(instance feild)
  //new 명령을 통해 Heap 메모리에 생성된다.
  String name;
  String email;
  String password;
  String tel;
}
